apiVersion: triliovault.trilio.io/v1
kind: Restore
metadata:
  name: {{ tvk_restore_name }}
spec:
  source:
    type: Backup
    backup:
      name: {{ tvk_restore_name }}
      namespace: {{ tvk_namespace }}
    restoreNamespace: {{ tvk_restore_namespace }}
    skipIfAlreadyExists: true
